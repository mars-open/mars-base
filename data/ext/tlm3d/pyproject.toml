[project]
name = "geopkg-to-geoparquet"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "geopandas>=1.0.0",
    "pyogrio>=0.9.0",  # Faster GPKG reader
    "fiona>=1.10.0",   # Layer discovery
    "pandas>=2.2.0",
    "pyarrow>=17.0.0"  # Parquet engine
]